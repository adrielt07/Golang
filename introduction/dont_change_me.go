package main

const (
      message = "Edit this file and uncomment the 'message' (remove //) inside main() or init()"
)

func main() {
//     message = "remove the //"
     println(message)
}

func init() {
//     message = "remove the //"
}